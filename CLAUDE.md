# CLAUDE.md — vr-racing

Personal static site (not MEcommerce). One file, `index.html`, plus `img/` and `og.png`; custom domain `vrracing.commercedwar.com`.

Rules: keep it a single dependency-free page (fonts from Google Fonts only; art is inline SVG); no analytics or forms that store data — booking goes through the WhatsApp deep link; every price or rule change is the owner's call and is reflected in `README.md`'s checklist. **Privacy is a hard rule:** no personal name, personal phone number or street address anywhere in the repo (Sector 38 only; the WhatsApp number is the business one, in `<body data-wa>`); images go through `tools/optimise_images.py`, which strips metadata. Commit prefixes as in the workspace (`feat:`, `fix:`, `docs:`, `chore:`). Design brief and decisions: `Claude generated files\three-tasks-2026-09-03\ANSWERS-2026-09-04.md` and `Claude generated files\vr-racing-round-2\` in the workspace.
